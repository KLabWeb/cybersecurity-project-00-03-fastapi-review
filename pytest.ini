[pytest]
# src for the app packages, . for data.legacy.* -- mirrors PYTHONPATH in the Dockerfile.
# Without this, `from main import app` fails from both the CLI and the VS Code test runner.
pythonpath = src .

testpaths = src/api/tests
